<Window x:Class="Sm64DecompLevelViewer.M64DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="M64 Sequence Structural Diagnostics" Height="600" Width="850"
        WindowStartupLocation="CenterOwner" Background="#1E1E1E" Foreground="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TitleLabel" Text="M64 Diagnostic Report" VerticalAlignment="Center" Foreground="#CCCCCC" FontSize="11" FontWeight="Bold"/>
                <Button Grid.Column="1" Click="CopyButton_Click" Background="#4F4F4F" Foreground="White" BorderThickness="0" Padding="10,5" FontSize="11">Copy Report</Button>
            </Grid>
        </Border>

        <TextBox Grid.Row="1" x:Name="DiagnosticsTextBox" FontFamily="Consolas" FontSize="12" Background="#1E1E1E" Foreground="#DCDCDC" BorderThickness="0" IsReadOnly="True" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="12"/>
    </Grid>
</Window>
