<Window x:Class="Sm64DecompLevelViewer.PatchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Decomp Patch Manager" Height="500" Width="950"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBackground" Color="#252526"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="#E57373"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F1F1F1"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F4F56"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                                <Setter Property="Foreground" Value="#9CDCFE"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2D2D30"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="🩹 DECOMP PATCH MANAGER" FontSize="16" FontWeight="Bold" Foreground="{StaticResource HeaderBrush}" Margin="0,0,0,15"/>

        <!-- Main Content split into Sidebar & Console -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Scanned patches list -->
            <GroupBox Header="Enhancements Folder Patches" BorderBrush="{StaticResource BorderBrush}" Background="{StaticResource PanelBackground}" Foreground="#CCCCCC" Grid.Column="0" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListBox x:Name="AvailablePatchesList" SelectionChanged="AvailablePatchesList_SelectionChanged" Margin="0,0,0,5"/>
                    <Button Grid.Row="1" Content="🔄 Rescan Folder" Click="RescanButton_Click" Padding="5" HorizontalAlignment="Stretch"/>
                </Grid>
            </GroupBox>

            <!-- Middle: Built-in C Patches -->
            <GroupBox Header="Randm64 C Patches" BorderBrush="{StaticResource BorderBrush}" Background="{StaticResource PanelBackground}" Foreground="#CCCCCC" Grid.Column="2" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="5">
                            <CheckBox x:Name="SkipGoddardCheck" Content="Skip Goddard Face" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                            <CheckBox x:Name="SkipTitleCheck" Content="Skip Title Screen" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                            <CheckBox x:Name="SkipLakituCheck" Content="Skip Lakitu Intro" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                            <CheckBox x:Name="Start99LivesCheck" Content="Start with 99 Lives" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                            <CheckBox x:Name="StageSelectCheck" Content="Enable Stage Select" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                            <CheckBox x:Name="AudioProtectionCheck" Content="Audio Protection Guard" Foreground="#CCCCCC" Margin="0,0,0,10" ToolTip="Prevents audio driver crashes, freezes, and muting due to out-of-bounds instrument, bank, or drum parameters."/>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <Button Grid.Row="1" Content="⚡ Apply Selected C Patches" Click="ApplyCPatches_Click" Padding="6" Background="#007ACC" Foreground="White" BorderThickness="0" FontWeight="SemiBold" HorizontalAlignment="Stretch"/>
                </Grid>
            </GroupBox>

            <!-- Right: Actions + Terminal -->
            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Selected Patch details -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Target Patch:" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" x:Name="PatchPathText" Background="#2D2D30" Foreground="White" BorderBrush="#3F3F46" Padding="6" IsReadOnly="False"/>
                    <Button Grid.Column="2" Content="Browse..." Click="BrowseButton_Click" Width="80" Margin="10,0,0,0" Padding="8,4"/>
                </Grid>

                <!-- Actions -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="🚀 Apply via git apply" Click="ApplyGit_Click" Background="#007ACC" FontWeight="Bold" Padding="12,6" Margin="0,0,10,0"/>
                    <Button Content="🛠️ Apply via patch command" Click="ApplyPatchCmd_Click" Padding="12,6"/>
                </StackPanel>

                <!-- Output Log -->
                <GroupBox Grid.Row="2" Header="Patch Command Log Output" BorderBrush="{StaticResource BorderBrush}" Background="{StaticResource PanelBackground}" Foreground="#CCCCCC" Padding="5">
                    <TextBox x:Name="LogOutputText" FontFamily="Consolas" FontSize="11" Background="#1E1E1E" Foreground="#85C46C" BorderThickness="0" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                </GroupBox>
            </Grid>
        </Grid>

        <!-- Close Button -->
        <Button Grid.Row="2" Content="Close" Click="CloseButton_Click" HorizontalAlignment="Right" Width="100" Margin="0,15,0,0" Padding="10,5"/>
    </Grid>
</Window>
