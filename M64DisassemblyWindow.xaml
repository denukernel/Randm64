<Window x:Class="Sm64DecompLevelViewer.M64DisassemblyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="M64 Sequence Disassembly" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" Background="#1E1E1E" Foreground="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="0,0,0,1" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PathLabel" Text="Disassembling..." VerticalAlignment="Center" Foreground="#CCCCCC" FontSize="11"/>
                <Button Grid.Column="1" Click="CopyButton_Click" Background="#4F4F4F" Foreground="White" BorderThickness="0" Padding="8,4" Margin="0,0,8,0" FontSize="11">Copy to Clipboard</Button>
                <Button Grid.Column="2" Click="RefreshButton_Click" Background="#4F4F4F" Foreground="White" BorderThickness="0" Padding="8,4" FontSize="11">Refresh</Button>
            </Grid>
        </Border>

        <TextBox Grid.Row="1" x:Name="DisassemblyTextBox" FontFamily="Consolas" FontSize="12" Background="#1E1E1E" Foreground="#DCDCDC" BorderThickness="0" IsReadOnly="True" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="10"/>
    </Grid>
</Window>
