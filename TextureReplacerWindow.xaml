<Window x:Class="Sm64DecompLevelViewer.TextureReplacerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Texture Replacer" Height="680" Width="850"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E" Foreground="#CCCCCC"
        ShowInTaskbar="False">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBackground" Color="#252526"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#1C97EA"/>
        
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" CornerRadius="4,4,0,0" Margin="2,0">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1E1E1E"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3F3F46"/>
                                <Setter Property="Foreground" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Text="Texture Replacer" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
        
        <!-- Main Tab Control -->
        <TabControl Grid.Row="1">
            <!-- Tab 1: Create Rules -->
            <TabItem Header="Create Replacement Rules">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Columns -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Left Column: Targets -->
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="80"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Target Category Filter:" Foreground="#999999" Margin="0,0,0,4"/>
                            <ComboBox x:Name="TargetCategoryComboBox" Grid.Row="1" Height="26" Margin="0,0,0,8" Background="#252526" Foreground="Black" BorderBrush="{StaticResource BorderBrush}" SelectionChanged="TargetCategoryComboBox_SelectionChanged"/>
                            
                            <TextBlock Text="Target Search (type name):" Grid.Row="2" Foreground="#999999" Margin="0,0,0,4"/>
                            <TextBox x:Name="TargetSearchTextBox" Grid.Row="3" Margin="0,0,0,8" Background="#333333" Foreground="White" BorderBrush="{StaticResource BorderBrush}" Padding="4" TextChanged="TargetSearchTextBox_TextChanged"/>
                            
                            <ListBox x:Name="TargetListBox" Grid.Row="4" SelectionMode="Multiple" Background="#2D2D30" Foreground="White" BorderBrush="{StaticResource BorderBrush}" PreviewKeyDown="TargetListBox_PreviewKeyDown" SelectionChanged="TargetListBox_SelectionChanged"/>
                            
                            <!-- Target Preview header -->
                            <TextBlock Text="Target Preview:" Grid.Row="5" Foreground="#999999" Margin="0,5,0,2"/>
                            
                            <!-- Target Preview panel -->
                            <Border Grid.Row="6" Background="#252526" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="64"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="54" Height="54" Background="#111111" BorderBrush="#444444" BorderThickness="1" SnapsToDevicePixels="True">
                                        <Image x:Name="TargetPreviewImage" Stretch="Uniform"/>
                                    </Border>
                                    <TextBlock x:Name="TargetPreviewText" Grid.Column="1" Text="No Selection" Foreground="#888888" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11" TextWrapping="Wrap"/>
                                </Grid>
                            </Border>
                        </Grid>
                        
                        <!-- Middle arrow -->
                        <Grid Grid.Column="1">
                            <TextBlock Text="&gt;&gt;" FontSize="20" FontWeight="Bold" Foreground="#444444" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        
                        <!-- Right Column: Replacement -->
                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="80"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Replacement Category Filter:" Foreground="#999999" Margin="0,0,0,4"/>
                            <ComboBox x:Name="ReplacementCategoryComboBox" Grid.Row="1" Height="26" Margin="0,0,0,8" Background="#252526" Foreground="Black" BorderBrush="{StaticResource BorderBrush}" SelectionChanged="ReplacementCategoryComboBox_SelectionChanged"/>
                            
                            <TextBlock Text="Replacement Search (type name):" Grid.Row="2" Foreground="#999999" Margin="0,0,0,4"/>
                            <TextBox x:Name="ReplacementSearchTextBox" Grid.Row="3" Margin="0,0,0,8" Background="#333333" Foreground="White" BorderBrush="{StaticResource BorderBrush}" Padding="4" TextChanged="ReplacementSearchTextBox_TextChanged"/>
                            
                            <ListBox x:Name="ReplacementListBox" Grid.Row="4" SelectionMode="Single" Background="#2D2D30" Foreground="White" BorderBrush="{StaticResource BorderBrush}" SelectionChanged="ReplacementListBox_SelectionChanged"/>
                            
                            <!-- Replacement actions and preview -->
                            <DockPanel Grid.Row="5" Margin="0,5,0,2">
                                <TextBlock Text="Replacement Preview:" Foreground="#999999" VerticalAlignment="Center"/>
                                <Button Content="📁 Browse Custom PNG..." Click="BrowseCustomPng_Click" HorizontalAlignment="Right" Background="#3E3E42" Foreground="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="8,2" Cursor="Hand" FontSize="10"/>
                            </DockPanel>
                            
                            <Border Grid.Row="6" Background="#252526" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="64"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="54" Height="54" Background="#111111" BorderBrush="#444444" BorderThickness="1" SnapsToDevicePixels="True">
                                        <Image x:Name="ReplacementPreviewImage" Stretch="Uniform"/>
                                    </Border>
                                    <TextBlock x:Name="ReplacementPreviewText" Grid.Column="1" Text="No Selection" Foreground="#888888" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11" TextWrapping="Wrap"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                    
                    <!-- Action button -->
                    <Button Content="Add Replacement Rule" Grid.Row="1" Margin="0,15,0,0" Height="36" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" FontWeight="Bold" Click="AddRuleButton_Click" Cursor="Hand"/>
                </Grid>
            </TabItem>
            
            <!-- Tab 2: Active Rules -->
            <TabItem Header="Active Rules List">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <ListBox x:Name="RulesListBox" Background="#2D2D30" Foreground="White" BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding TargetName}" FontWeight="SemiBold" Foreground="#E0E0E0"/>
                                    <TextBlock Text="  ====&gt;  " Grid.Column="1" Foreground="#007ACC" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding ReplacementName}" Grid.Column="2" Foreground="#A0A0A0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <Button Content="Remove Selected Rule(s)" Grid.Row="1" Height="32" Background="#A11A1A" Foreground="White" BorderThickness="0" FontWeight="Bold" Click="RemoveRuleButton_Click" Cursor="Hand"/>
                </Grid>
            </TabItem>
        </TabControl>
        
        <!-- Bottom bar -->
        <DockPanel Grid.Row="2" Margin="0,15,0,0">
            <TextBlock x:Name="StatusLabel" Text="Ready" VerticalAlignment="Center" Foreground="#888888" FontStyle="Italic"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save &amp; Apply" Click="SaveButton_Click" Width="120" Margin="0,0,10,0"
                        Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" Padding="8" FontWeight="Bold" Cursor="Hand"/>
                <Button Content="Cancel" Click="CancelButton_Click" Width="100"
                        Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="8" Cursor="Hand"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
